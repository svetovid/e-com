akka {
    actor {
        provider = cluster
    }

    remote {
        dot-netty.tcp {
            hostname = "127.0.0.1"
            port = 45066
        }
    }

    cluster {
        seed-nodes = ["akka.tcp://paassystem@127.0.0.1:45053"]
        roles = [paas-instance]
    }
}